const baseUrl = 'https://designer.fstrk.io';

export default {
  baseUrl,
  api: {
    basePath: baseUrl + '/api',
    headers: {
      // Accept: 'application/json; camelcase=0, */*',
      // 'Content-Type': 'application/json; camelcase=1',
    }
  }
};
